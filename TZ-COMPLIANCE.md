# Соответствие техническому заданию

Ниже — каждый пункт ТЗ и то, чем он закрыт в коде. Столбец «проверено» —
автотест, фиксирующий поведение (`python -m pytest tests/ -q`, 149 тестов).

## Функции агента

### Знать проект OPERON

| Требование | Реализация | Проверено |
|---|---|---|
| Подключаться к базе знаний | Каталог `knowledge_base/`, индекс собирается при старте и пересобирается при изменении файлов. Читаются не только текстовые форматы, но и **`.docx`, `.pdf`, `.xlsx`, `.pptx`** — реальные договоры и регламенты лежат именно в них. Разбор в процессе, без внешних сервисов; сканы и устаревшие `.doc`/`.xls` не молчат, а называют причину. Инструменты `kb_search`, `kb_get_document`, `kb_list_documents` | `test_knowledge_base.py`, `test_documents.py` |
| Понимать бизнес-модель, продукты, партнёров, договоры, процессы, тарифы, KPI, задачи | Разделы базы знаний с готовыми шаблонами под каждый тип документа; поиск понимает русскую морфологию (запрос «тарифы партнёров» находит «тариф партнёра») | `TestSearch`, `TestMorphology` |
| Быстро находить информацию | BM25 по фрагментам с бустом за совпадение в заголовке; markdown режется по разделам | `TestIndexing` |
| Отвечать с указанием источника | Каждый результат несёт готовую ссылку: файл, раздел, дата обновления | `test_result_carries_citation_fields` |
| Пополнять базу знаний из чата | Документ, присланный боту файлом, скачивается, разбирается и показывается человеку **до** сохранения — в базу попадает только после подтверждения. Разбор до записи намеренно: файл, из которого не достать текст, поиском не найдётся, но выглядел бы добавленным. Имя файла приходит извне и обезвреживается: «../../etc/passwd.md» → «passwd.md» | `test_intake.py`, `TestDocumentUpload` |

### Работать с Google Drive

| Требование | Реализация | Проверено |
|---|---|---|
| Находить и читать документы, таблицы, презентации | `drive_search`, `drive_read` (Документы и Презентации — текстом, Таблицы — полистно через Sheets API, без него — выгрузкой .xlsx через Диск) | `TestGoogleDegradation`, `TestSpreadsheetFallback` |
| Сравнивать версии | `drive_list_revisions` + `drive_compare_revisions` — построчная разница между двумя редакциями | — |
| Анализировать данные, готовить сводки, отчёты, расчёты, КП, проекты документов | Прочитанные данные обрабатывает модель; готовый результат сохраняется на Диск через `drive_create_file` (документ, таблица из CSV, текст) | `test_every_write_tool_is_gated` |

### Работать с Google Calendar

| Требование | Реализация | Проверено |
|---|---|---|
| Видеть встречи и сроки | `calendar_list_events` (основной календарь; отдельный список календарей не запрашиваем, чтобы не расширять права) | `TestCalendarParsing` |
| Создавать события и напоминания **после подтверждения** | `calendar_create_event` / `update` / `delete` помечены `requires_confirmation` — рантайм останавливает ход и ждёт решения | `TestWriteToolsAreGated` |
| Напоминать о задачах, отчётности, контрольных точках | **Бот пишет сам**: планировщик `app/reminders.py` на пульсе опроса Telegram шлёт утреннюю сводку (просрочки, близкие сроки, встречи дня). Собирается без модели — это отчёт, а не текст; отправляется ровно один раз (ключи на диске переживают передеплой); молчит, когда сказать нечего. Плюс событие календаря с `reminder_minutes` для точечных напоминаний | `test_reminders.py`, `TestProactiveReminders` |

### Бизнес-ассистент

| Требование | Реализация | Проверено |
|---|---|---|
| Фиксировать поручения и контролировать дедлайны | `task_create`, `task_update`, `tasks_list`; хранение в `data/tasks.json`, расчёт просрочки и остатка дней | `TestTasks` |
| Протоколы встреч и списки следующих действий | `protocol_save`: модель разбирает расшифровку на договорённости / решения / поручения / открытые вопросы, инструмент сохраняет протокол **и заводит поручения в реестр** — оттуда их видят контроль сроков и утренняя сводка. Одно подтверждение на весь протокол. Ненайденные ответственный и срок не додумываются, а отмечаются отдельно | `TestProtocolBecomesTasks` |
| Анализировать показатели, выявлять отклонения, риски, узкие места | Реестр `kpi_list` / `kpi_upsert`: план, факт, направление показателя и пороги реагирования. **Статус (ok / warning / critical) считает код, а не модель** — промпт прямо запрещает переопределять вывод. Учитывается направление: для выручки плохо отставание, для стоимости привлечения — превышение | `TestDeviationIsComputed`, `TestKpiRegistry` |
| Предлагать решения и аргументировать | Промпт требует связки «отклонение → причина → что делать» поверх посчитанного статуса: объяснять и предлагать — можно, менять вывод инструмента — нельзя | раздел «Показатели и отклонения» |

### Использовать интернет

| Требование | Реализация | Проверено |
|---|---|---|
| Искать данные о рынке, конкурентах, законодательстве, технологиях | При прямом доступе к Anthropic — серверный `web_search`. Через сторонний шлюз — собственные `internet_search` (бесплатно, без ключей: DuckDuckGo → Bing, Google News, Википедия; либо платный API) и `open_url` | `test_web_tools.py`, `test_free_search.py` |
| Отделять внутреннюю информацию от внешней | Результаты поиска помечены `source_type: internet` и явной инструкцией не смешивать источники; в промпте три источника описаны как разные категории | `test_results_carry_links_and_retrieval_date` |
| Указывать ссылки, источники и дату актуальности | Каждый результат несёт ссылку и `retrieved_at`; `open_url` возвращает готовую строку цитирования | `test_citation_includes_url_and_date` |

## Требования

| Требование | Реализация | Проверено |
|---|---|---|
| Интерфейс — простой чат-бот | Одна страница, поле ввода и лента сообщений. Без сборки и внешних зависимостей | `test_static_assets.py` |
| (сверх ТЗ) Mini App: выбор модели, баланс и стоимость | Вкладка «Модель и баланс»: каталог, цены и баланс из API RouterAI; смена модели — после подтверждения; вход по подписи Telegram и белому списку | `test_miniapp.py` |
| Ответы краткие, структурированные, ориентированные на задачу | Раздел промпта «Формат ответа»: вывод в первом предложении, списки и таблицы по делу, без пересказа вопроса | — |
| **Не придумывать отсутствующие данные** | Пустой результат любого инструмента возвращает модели прямой запрет домысливать. Промпт запрещает достраивать суммы, даты, имена, номера договоров | `test_empty_kb_returns_explicit_hint_not_silence`, `test_without_key_tells_model_not_to_invent` |
| При недостатке информации — уточняющие вопросы | Промпт задаёт критерий: спрашивать, когда разные трактовки дают принципиально разный результат; иначе — явно обозначенное допущение | — |
| Доступ к документам и календарю — в рамках прав пользователя | OAuth-токен конкретного человека, без сервисных аккаунтов. Отказ Google (403) передаётся как есть, обхода нет | `TestGoogleDegradation` |
| **Создание, изменение и отправка — только после подтверждения** | Флаг `requires_confirmation` у инструмента; рантайм не выполняет вызов, а замораживает ход и отдаёт карточку. Отсутствие ответа = отказ, и отказ наступает **по времени**: через `OPERON_CONFIRMATION_TTL_MINUTES` (30 мин) действие отменяется само, модель получает «не выполнено», диалог освобождается | `TestWriteToolsAreGated`, `test_missing_decision_defaults_to_reject`, `TestExpiryClosesTheTurn` |
| «На любой базе» | Два протокола шлюза: Anthropic Messages API и OpenAI Chat Completions. Провайдер и протокол задаются переменными окружения, код агента одинаков | `test_openai_backend.py` |

## Где требования закреплены архитектурно, а не текстом промпта

Два пункта ТЗ нельзя доверять формулировке в промпте — модель может её не
исполнить. Они вынесены в код:

1. **Подтверждение изменений.** Решение принимает рантайм по флагу инструмента.
   Даже если модель «решит» действовать сама, вызов не выполнится: ход
   останавливается, пользователь видит карточку с описанием действия.
   `test_every_write_tool_is_gated` следит, чтобы новый изменяющий инструмент
   не появился в обход шлюза.

2. **Запрет на выдумывание.** Инструменты возвращают не пустоту, а явный
   статус `empty` / `not_found` вместе с инструкцией, что делать. Модели не
   приходится угадывать, есть данные или нет.
