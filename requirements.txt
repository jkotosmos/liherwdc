# --- Ядро агента ---
# httpx объявлен явно: на нём построены свой шлюз к модели, клиент Telegram
# и интернет-поиск. Раньше он приезжал попутно с anthropic, но начиная с
# версии 1.0 та перешла на httpx2 — и установка ломалась на чистой машине.
# Прямая зависимость должна быть записана прямо.
httpx>=0.27
# База часовых поясов IANA. На Linux она есть в системе, на Windows её нет
# вообще: без этого пакета ZoneInfo("Europe/Moscow") не находится, настройки
# молча подставляют UTC, и все сроки, «сегодня» и час утренней сводки
# уезжают на три часа. Ошибка тихая — тем и опасна.
tzdata>=2024.1
anthropic>=0.122.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-dotenv>=1.0.1

# --- Разбор офисных документов ---
# Договоры, регламенты и тарифы лежат в .docx и .pdf, а не в markdown.
# Разбор идёт в процессе, без внешних сервисов.
pypdf>=6.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0

# --- Интеграции Google (опционально: без них агент работает,
#     но инструменты Drive/Calendar сообщают о недоступности) ---
google-api-python-client>=2.150.0
google-auth>=2.35.0
google-auth-oauthlib>=1.2.1
cryptography>=41.0
